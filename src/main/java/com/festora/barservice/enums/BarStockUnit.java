package com.festora.barservice.enums;

public enum BarStockUnit {
    BOTTLE,     // Full bottle tracking (e.g. whiskey)
    PINT,       // Beer pints
    ML,         // Millilitre (pour tracking)
    UNIT        // Cans, packaged drinks
}
